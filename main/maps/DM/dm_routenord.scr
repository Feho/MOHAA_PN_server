// Map DM_ROUTENORD
//
// Developed by Jeff 'Dr ABAP' Owen (owenjg@hotmail.com) 2002
// The map is a DM/TDM
//
// Adapted to the PN server: mirrors maps/DM/mohdm4.scr so this custom map
// gets tickets, squadmaker and support gunners like the stock DM maps.
// Loose file (capital DM dir, matching mohdm*.scr) overrides the pk3 copy.

main:

	// Random client crash fix
	removeclass PlayerIntermission

	setcvar "g_gametype" "4"
	setcvar "timelimit" "15"
	setcvar "fraglimit" "0"

	// set scoreboard messages
	setcvar "g_obj_alliedtext1" "Route du Nord"
	setcvar "g_obj_alliedtext2" "Map type: DM/TDM"
	setcvar "g_obj_alliedtext3" ""
	setcvar "g_obj_axistext1" ""
	setcvar "g_obj_axistext2" ""
	setcvar "g_obj_axistext3" ""

	setcvar "g_scoreboardpic" "dm_routenord"

	// load support gun sounds
	waitthread global/support/support.scr::soundcache

	// call additional stuff for playing this map round based is needed
	if(level.roundbased)
		thread roundbasedthread

	level waittill prespawn

	//*** Precache Dm Stuff
	exec global/DMprecache.scr

	exec global/door_locked.scr::lock
	level.script = maps/dm/dm_routenord.scr
	exec global/ambient.scr mohdm4

	//*** Fog Parms
	$world farplane 12000
	$world farplane_color (.333 .333 .349)

	level waittill spawn

	level.squadmaker_areaname = "on the north road"

	// start support gunner scripts
	exec global/support/support.scr

	// start squadmaker
	exec global/squadmaker/squadmaker.scr

	// start tickets mode
	exec global/feho/tickets.scr

end

//-----------------------------------------------------------------------------

roundbasedthread:

	// Can specify different scoreboard messages for round based games here.

	level waitTill prespawn

	level waittill spawn

	// set the parameters for this round based match
	level.dmrespawning = 0 // 1 or 0
	level.dmroundlimit = 5 // round time limit in minutes
	level.clockside = kills // set to axis, allies, kills, or draw

	level waittill roundstart

end
